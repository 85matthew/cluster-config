apiVersion: config.openshift.io/v1
kind: Authentication
metadata:
  annotations:
    release.openshift.io/create-only: "true"
  name: cluster
  namespace: pod-identity-webhook-custom
spec:
  serviceAccountIssuer: https://s3.us-east-1.amazonaws.com/napsocp2iam
